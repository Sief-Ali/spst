#ifndef APP_H
#define APP_H

/* Initializes application-level modules before entering the main loop. */
void APP_Init(void);

/* Runs the application forever after initialization is complete. */
void APP_Run(void);

#endif
